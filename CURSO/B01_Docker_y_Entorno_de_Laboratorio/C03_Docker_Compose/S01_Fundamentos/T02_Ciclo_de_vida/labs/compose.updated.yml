services:
  web:
    image: nginx:alpine
    ports:
      - "9090:80"

  worker:
    image: alpine:latest
    command: sh -c 'i=0; while true; do i=$((i+1)); echo "Worker iteration $$i"; sleep 3; done'
